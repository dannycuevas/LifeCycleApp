@model IEnumerable<LifecycleLibrary.PersonModel>

@{
    ViewBag.Title = "About";
}
<h2>@ViewBag.Title.</h2>
<h3>@ViewBag.Message</h3>

<p>Use this area to provide additional information like:</p>
@foreach (var person in Model)
{
    <div>
        @person.Id - @person.FirstName @person.LastName
    </div>
}
